# 设计与验收记录

## 视觉基准

本项目使用内置 Image Gen 生成三段高保真界面概念，作为实现基准：

1. 顶部段：精简封面、此刻关注、全程总览与底部导航；要求暖白纸张、深绿标题、日落橙和黄铜金、原创寺庙／渡船线稿，不使用旅游大图。
2. 中段：交通住宿票卡、酒店、待订船班、D1—D5 日期栏与 D3 连续时间轴；要求票卡和时间轴使用不同信息密度，不做同质卡片网格。
3. 尾段：共享待办、实用信息、预算与参考资料；要求开放列表、单一重点预算面板和少量线稿插图。新增行李清单沿用这一段的开放列表语言，不另造一套视觉系统。

实际页面以网站制作说明中的日期、地点、状态和文字为准，未沿用概念图中自动生成的错误内容。

## 设计系统

- 背景 `#F7F3EB`；卡片 `#FFFDFA`；正文 `#26372F`；主色 `#214D43`；强调 `#D77952`；装饰金 `#B69250`；边框 `#DEDCD3`。
- 中文系统无衬线；正文 16px／1.65；标题按视口缩放；时间和倒计时使用等宽数字。
- 手机 16—20px 页面留白；卡片 16—22px 圆角；交互区不小于 44px。
- 深色主题使用独立深灰绿配色，而非简单反色。

## 视觉一致性账本

| 对比项 | 概念基准 | 实际渲染 | 处理结果 |
| --- | --- | --- | --- |
| 首屏层级 | 标题、日期路线、线稿、关注卡依次出现 | 桌面与窄屏保持同一层级 | 通过 |
| 配色与背景 | 暖白、深绿、橙、黄铜金；无明显纸纹 | 色值锁定为规格变量，纸纹仅为几乎不可见的微粒 | 通过 |
| 关注卡 | 强倒计时、下一项、双操作 | 绝对时间计算；数字固定宽度；未确认船班不会显示成已订 | 通过 |
| 信息容器 | 票券、酒店块、开放时间轴采用不同构图 | 未退化成重复卡片网格 | 通过 |
| D3 时间轴 | 日期栏固定，时间轴为主要阅读模式 | 10:30 大皇宫、步行、渡船和晚餐衔接完整 | 通过 |
| 尾段节奏 | 待办开放列表、单一预算面板 | 待办无本地勾选框，预算明确“非实际支出” | 通过 |
| 手机底栏 | 入口居中、选中态清楚 | 新增独立“行李”入口；320／390／430px 无横向溢出 | 通过 |
| 锚点与阅读状态 | 指定日期定位且标题不被遮挡 | 修复横向日期栏打断纵向定位；深链即时落位 | 通过 |
| 每日地图路线 | 展开后应直接进入真实地图路线 | 使用 Google Maps Directions URL 写入起点、途经点和终点；跨境日与超出手机途经点上限的返程分段 | 通过 |
| 手机首页动线 | 此刻关注后直接进入主要行程 | 删除重复的全程总览；逐日攻略前移到关注卡之后；底栏首项改为“首页”并准确返回顶部 | 通过 |
| 行李清单 | 暖白、深绿与开放列表；突出进度和最后确认 | 三清单分段标签、浅橙临出门标记、粘性总进度和单一主按钮；没有重复卡片墙 | 通过 |

## 浏览器检查

- 320px、390px、430px 和桌面宽度：无横向溢出。
- D3 与实用信息深链：顶部定位正确，日期／底部导航高亮正确。
- 浅色、深色、跟随系统：切换正常并保存阅读偏好。
- 折叠区、复制、日期分享、地图链接、焦点样式与减少动态效果均已实现。
- 最终浏览器控制台：无 error 或 warning。
- 行李模块实测：19 条初始物品、按条目计数、未装包筛选、增删改与撤销、共同物品携带人、遗漏三分组、临出门双向勾选均正常。
- 共享数据库未连接时明确显示演示模式；不会显示“已同步”，离线时不会提交修改。
